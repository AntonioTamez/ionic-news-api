import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-news-api',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
